server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:6541/delivery
    driver-class-name: org.postgresql.Driver
    username: delivery_user
    password: password6
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
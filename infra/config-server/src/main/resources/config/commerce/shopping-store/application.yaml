server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:6541/smarthome
    driver-class-name: org.postgresql.Driver
    username: smart
    password: home
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

logging:
  level:
    org:
      zalando:
        logbook: TRACE
server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:6541/warehouse
    driver-class-name: org.postgresql.Driver
    username: warehouse_user
    password: password4
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
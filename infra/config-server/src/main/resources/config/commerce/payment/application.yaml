server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:6541/payment
    driver-class-name: org.postgresql.Driver
    username: payment_user
    password: password7
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always